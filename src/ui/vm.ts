// The view model is a large, loosely shaped object built in viewModel.ts.
// Screens read from it by path, exactly as the prototype templates did.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type VM = any;
